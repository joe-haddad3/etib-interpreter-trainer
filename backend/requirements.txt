# ============================================================
# ETIB Platform — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core framework ---
flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# --- Free LLM clients ---
groq==0.9.0                  # Groq free tier (Llama 3.1 70B) — primary LLM
google-generativeai==0.7.2   # Gemini free tier — secondary LLM
ollama==0.2.1                # Local Ollama client — offline fallback

# --- TTS (Text-to-Speech) ---
edge-tts==6.1.9              # Microsoft Edge voices — no API key needed, free

# --- ASR (Automatic Speech Recognition) ---
faster-whisper==1.0.3        # Best free Arabic ASR — runs locally

# --- Audio processing ---
pydub==0.25.1                # Audio file manipulation
ffmpeg-python==0.2.0         # ffmpeg bindings (requires ffmpeg system install)

# --- File generation ---
python-docx==1.1.0           # DOCX glossary export (requirement B11)
pypdf==4.2.0                 # PDF text extraction for document-grounded speech generation
fpdf2==2.7.9                 # PDF generation

# --- Database ---
# SQLite is built into Python — no install needed
# Flask-SQLAlchemy for ORM
flask-sqlalchemy==3.1.1
pymongo==4.10.1

# --- Testing ---
pytest==8.2.0
pytest-flask==1.3.0

# --- Utilities ---
requests==2.32.3
